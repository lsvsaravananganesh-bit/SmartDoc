"use client";
import {useState} from 'react';
import Shell from '../components/Shell';
export default function Upload(){const [file,setFile]=useState<File|null>(null);return <Shell><div className="top"><div><div className="eyebrow">Step 1 of 4</div><h1 className="title">Upload data</h1><div className="muted">Bring your Excel or CSV file. We will map the fields for personalization.</div></div></div><section className="card"><div className="drop"><h2>{file?'File selected':'Drop your spreadsheet here'}</h2><p className="muted">Supported: .xlsx, .xls, .csv</p><input type="file" accept=".csv,.xlsx,.xls" onChange={e=>setFile(e.target.files?.[0]||null)}/>{file&&<p><b>{file.name}</b> — ready for validation.</p>}</div><div className="actions" style={{marginTop:18}}><a className="btn secondary" href="/">Cancel</a><a className="btn primary" href="/templates">Continue to templates →</a></div></section><div className="layout2"><section className="card"><h3 className="sectionTitle">Validation rules</h3><p className="muted">Required: a unique recipient/name field. Email is required only when you plan to send a campaign.</p></section><section className="card"><h3 className="sectionTitle">Example columns</h3><p><span className="badge">name</span> <span className="badge">email</span> <span className="badge">company</span> <span className="badge">role</span></p></section></div></Shell>}
