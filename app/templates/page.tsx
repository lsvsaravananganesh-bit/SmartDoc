"use client";
import {useState} from 'react';
import Shell from '../components/Shell';
export default function Templates(){const [name,setName]=useState('Internship Certificate');return <Shell><div className="top"><div><div className="eyebrow">Step 2 of 4</div><h1 className="title">Template builder</h1><div className="muted">Design once, personalize every row.</div></div><a className="btn primary" href="/documents">Generate documents →</a></div><div className="layout2"><section className="card"><label className="label">Template name</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/><label className="label">Document content</label><div className="template"><h2>Certificate of Internship</h2><p>This is to certify that <b>{'{{name}}'}</b> has successfully completed an internship at <b>{'{{company}}'}</b> as a <b>{'{{role}}'}</b>.</p><p>We appreciate the contribution and wish them success in future endeavors.</p><p>Authorized by<br/><b>SmartDoc Organization</b></p></div></section><section className="card"><h3 className="sectionTitle">Available fields</h3><div className="actions">{['{{name}}','{{email}}','{{company}}','{{role}}','{{date}}'].map(x=><button className="btn secondary" key={x} onClick={()=>navigator.clipboard?.writeText(x)}>{x}</button>)}</div><h3 className="sectionTitle" style={{marginTop:24}}>AI assistant</h3><p className="muted">AI integration placeholder: generate a professional template from a short description, then review before using it.</p><button className="btn primary">Generate with AI</button></section></div></Shell>}
